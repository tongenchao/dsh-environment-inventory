# @dsh-external/proxy-pick

探测本机代理端口（7890/7897）可用性，一键把 git 全局代理切到可用端口

由 dsh-super-injector dev_scaffold_plugin 生成。

## 构建与注入

```bash
DSH_CHECKOUT=<checkout> bash scripts/build.sh
# 注入器环境内：dev_inject_plugin <本目录>
```
